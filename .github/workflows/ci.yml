name: CI

on: [push, pull_request]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  # Database Configuration
  PG_CONNECTION_STRING: postgresql://postgres:postgres@localhost:5432/postgres
  PGHISTORY_S3_ENDPOINT: http://localhost:9000
  PGHISTORY_S3_ACCESS_KEY_ID: root
  PGHISTORY_S3_SECRET_ACCESS_KEY: password
  PGHISTORY_S3_REGION: us-west-1
  PGHISTORY_S3_BUCKET: test-bucket

jobs:
  test:
    name: Test on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    services:
      postgres:
            # Pin to the same major the project targets (docker-compose uses
            # postgres:16-alpine). An unpinned `postgres` could run a newer major
            # and mask version-specific SQL behavior.
            image: postgres:16-alpine
            env:
              POSTGRES_USER: postgres
              POSTGRES_PASSWORD: postgres
              POSTGRES_DB: postgres
            options: >-
                  --health-cmd pg_isready
                  --health-interval 10s
                  --health-timeout 5s
                  --health-retries 5
            ports:
                  # Maps tcp port 5432 on service container to the host
                  - 5432:5432
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]

    steps:
      - name: Setup Minio
        # Pinned MinIO release (not :latest) so archival/S3 tests run against a
        # fixed server version and builds stay reproducible.
        run: |
          docker run -d -p 9000:9000 --name minio -e "MINIO_ROOT_USER=root" -e "MINIO_ROOT_PASSWORD=password" -v /tmp/data:/data -v /tmp/config:/root/.minio minio/minio:RELEASE.2024-01-16T16-07-38Z server /data
          # Wait for Minio to be ready. FAIL the job if it never comes up — otherwise
          # the S3/archival integration tests would silently skip and a regression
          # in that code path could merge unnoticed.
          timeout 60 bash -c 'until curl -f http://localhost:9000/minio/health/live; do sleep 2; done' || { echo "MinIO failed to start"; docker logs minio || true; exit 1; }

      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          # Pinned (not latest) so CI runs against a fixed Bun version.
          bun-version: 1.3.14

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Check
        run: bun run check

      - name: Build
        run: bun run build

  dashboard:
    name: Dashboard
    runs-on: ubuntu-latest
    # No postgres or MinIO service: every dashboard page is force-dynamic, so
    # `next build` compiles and typechecks without ever opening a connection.
    # Linting is already covered — the `check` job's `biome check .` sweeps the
    # whole repo, dashboard/ included. What is missing without this job is the
    # dashboard's own tsc (it is excluded from the root tsconfig, having its
    # own) and its build.
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14

      # The dashboard consumes pghistory through its published exports map via
      # a `file:..` link, so the library's own dependencies must be present
      # before its dist/ can be built.
      - name: Install library dependencies
        run: bun install --frozen-lockfile

      # `file:..` installs a *copy* of the parent, snapshotted at install time —
      # not a symlink. dist/ is gitignored, so on a clean checkout that copy has
      # no dist/ and `pghistory/next` resolves to nothing. Building the library
      # first puts dist/ in place before the copy is taken. The `pretsc`/
      # `prebuild` hooks rebuild the parent, but they write to ../dist, which the
      # already-copied node_modules/pghistory never sees.
      - name: Build library
        run: bun run build

      - name: Install dashboard dependencies
        run: bun install --frozen-lockfile
        working-directory: dashboard

      - name: Typecheck
        run: bun run tsc
        working-directory: dashboard

      # `prebuild` builds the parent package first, so this also proves the
      # dist/ the dashboard imports is the one the library currently produces.
      - name: Build
        run: bun run build
        working-directory: dashboard